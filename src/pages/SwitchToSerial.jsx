import { Box, Text } from "@chakra-ui/react";

const SwitchToSerial = () => {
  return (
    <Box p={4}>
      <Text fontSize="2xl" fontWeight="bold">Switch to Serial</Text>
      <Text mt={4}>This is the Switch to Serial page.</Text>
    </Box>
  );
};

export default SwitchToSerial;